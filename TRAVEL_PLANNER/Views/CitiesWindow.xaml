<Window x:Class="TRAVEL_PLANNER.Views.CitiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TRAVEL PLANNER"
        Width="1380"
        Height="860"
        MinWidth="1200"
        MinHeight="760"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource BaseWindowStyle}">
    <Border Background="{DynamicResource PageOverlayBrush}">
        <DockPanel>
            <Border DockPanel.Dock="Top"
                    Background="{StaticResource PrimaryBrush}"
                    Padding="24,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="TRAVEL PLANNER"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="White"
                               VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <Button Content="Головна"
                                Style="{StaticResource NavButtonStyle}"
                                Click="Main_Click"/>
                        <Button Content="Країни"
                                Style="{StaticResource NavButtonStyle}"
                                Click="Countries_Click"/>
                        <Button Content="Міста"
                                Style="{StaticResource NavButtonStyle}"
                                FontWeight="Bold"
                                Click="Cities_Click"/>
                        <Button Content="Налаштування"
                                Style="{StaticResource NavButtonStyle}"
                                Click="Settings_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Margin="28">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="420"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Міста"
                                           Style="{StaticResource TitleTextStyle}"
                                           FontSize="28"/>

                                <TextBlock Margin="0,20,0,0"
                                           FontWeight="SemiBold"
                                           Text="Обрана країна:"/>
                                <TextBlock x:Name="CountryNameTextBlock"
                                           Margin="0,8,0,0"
                                           FontSize="18"
                                           FontWeight="SemiBold"/>

                                <TextBlock Margin="0,20,0,0"
                                           FontWeight="SemiBold"
                                           Text="Оберіть місто зі списку:"/>
                                <ComboBox x:Name="CityComboBox"
                                          Margin="0,8,0,0"
                                          Height="38"
                                          DisplayMemberPath="place"
                                          SelectionChanged="CityComboBox_SelectionChanged"/>

                                <TextBlock Margin="0,20,0,0"
                                           FontWeight="SemiBold"
                                           Text="Тривалість подорожі"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Margin="0,0,10,0">
                                        <TextBlock Margin="0,10,0,0"
                                                   Text="Від:"/>
                                        <DatePicker x:Name="DatePickerFrom"
                                                    Style="{StaticResource DatePickerStyle}"
                                                    SelectedDateChanged="DatePicker_SelectedDateChanged"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Margin="0,10,0,0"
                                                   Text="До:"/>
                                        <DatePicker x:Name="DatePickerTo"
                                                    Style="{StaticResource DatePickerStyle}"
                                                    SelectedDateChanged="DatePicker_SelectedDateChanged"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Margin="0,20,0,0"
                                           FontWeight="SemiBold"
                                           Text="Вид подорожі"/>
                                <CheckBox x:Name="CheckBoxSea"
                                          Style="{StaticResource CheckBoxStyle}"
                                          Content="💓Серце міста"
                                          Checked="Filters_Changed"
                                          Unchecked="Filters_Changed"/>
                                <CheckBox x:Name="CheckBoxNature"
                                          Style="{StaticResource CheckBoxStyle}"
                                          Content="Парки та сквери"
                                          Checked="Filters_Changed"
                                          Unchecked="Filters_Changed"/>
                                <CheckBox x:Name="CheckBoxActivities"
                                          Style="{StaticResource CheckBoxStyle}"
                                          Content="Культурна спадщина"
                                          Checked="Filters_Changed"
                                          Unchecked="Filters_Changed"/>
                                <CheckBox x:Name="CheckBoxHistoric"
                                          Style="{StaticResource CheckBoxStyle}"
                                          Content="Історична пам'ятка"
                                          Checked="Filters_Changed"
                                          Unchecked="Filters_Changed"/>
                                <CheckBox x:Name="CheckBoxShopping"
                                          Style="{StaticResource CheckBoxStyle}"
                                          Content="Пам'ятка архітектури"
                                          Checked="Filters_Changed"
                                          Unchecked="Filters_Changed"/>

                                <TextBlock Margin="0,22,0,0"
                                           FontWeight="SemiBold"
                                           Text="ОСНОВНА ІНФОРМАЦІЯ"/>
                                <TextBox x:Name="TextBox_Info"
                                         Height="170"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         IsReadOnly="True"
                                         VerticalScrollBarVisibility="Auto"
                                         Style="{StaticResource InputTextBoxStyle}"/>

                                <Button x:Name="SaveButton"
                                        Margin="0,20,0,0"
                                        Height="46"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Click="SaveButton_Click"
                                        Content="Зберегти"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer Grid.Column="2"
                              VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="260"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        CornerRadius="18"
                                        Background="{DynamicResource SecondaryCardBrush}"
                                        Padding="12">
                                    <Image x:Name="CityMapImage"
                                           Stretch="Uniform"/>
                                </Border>

                                <StackPanel Grid.Column="2">
                                    <TextBlock x:Name="SelectedCityNameTextBlock"
                                               FontSize="28"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Margin="0,10,0,0"
                                               Style="{StaticResource SubtitleTextStyle}"
                                               Text="Ось список місць, які вам точно потрібно відвідати в цьому місті:"/>
                                    <TextBlock x:Name="SelectedCityShortInfoTextBlock"
                                               Margin="0,16,0,0"
                                               Style="{StaticResource SubtitleTextStyle}"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="320"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        CornerRadius="18"
                                        Background="{DynamicResource SecondaryCardBrush}">
                                    <Image x:Name="SelectedCityImage"
                                           Stretch="UniformToFill"/>
                                </Border>

                                <StackPanel Grid.Column="2">
                                    <TextBlock FontSize="22"
                                               FontWeight="SemiBold"
                                               Text="Деталі міста"/>
                                    <TextBlock x:Name="SelectedCityCategoryTextBlock"
                                               Margin="0,12,0,0"
                                               Foreground="{DynamicResource MutedTextBrush}"/>
                                    <TextBlock x:Name="SelectedCityDescriptionTextBlock"
                                               Margin="0,14,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </DockPanel>
    </Border>
</Window>
